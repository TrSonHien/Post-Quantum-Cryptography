# Simulation

Use this folder for simulation infrastructure and generated simulation artifacts.

- `scripts/` stores Makefiles, filelists, and run scripts.
- `logs/` stores simulation logs.
- `waves/` stores waveform databases.
- `outputs/` stores generated simulation output files.

Keep large generated artifacts out of commits unless intentionally documented.
