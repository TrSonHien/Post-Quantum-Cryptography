`timescale 1ns/1ps
module kpke_ciphertext_unpack_pipe(input wire clk,input wire rst_n,input wire start,output wire busy,output wire done,output wire error,input wire in_valid,output wire in_ready,input wire[31:0]in_data,input wire[3:0]in_keep,input wire in_last,output wire out_valid,input wire out_ready,output wire[31:0]out_data,output wire[3:0]out_keep,output wire out_last,output wire c2_segment,input wire zeroize_req,output wire zeroize_busy,output wire zeroize_done);
 kpke_format_pipe #(.WORDS(272),.SPLIT_WORDS(240))u(clk,rst_n,start,busy,done,error,in_valid,in_ready,in_data,in_keep,in_last,out_valid,out_ready,out_data,out_keep,out_last,c2_segment,zeroize_req,zeroize_busy,zeroize_done);endmodule
