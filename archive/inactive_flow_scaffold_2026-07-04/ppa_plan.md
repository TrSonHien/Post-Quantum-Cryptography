# PPA Plan

Use this file for area, timing, power, and throughput exploration plans.

Do not record PPA claims until reports exist under `reports/` or tool-flow outputs exist under `synth/` and the server-controlled `pd/` workspace.
